name: psy_agent
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    # ── Web 框架 ──────────────────────────────────────────────────────────────
    - fastapi==0.135.3
    - uvicorn[standard]==0.34.3
    - starlette==1.0.0
    - python-multipart==0.0.27
    # ── 数据库 ────────────────────────────────────────────────────────────────
    - sqlalchemy[asyncio]==2.0.49
    - asyncpg==0.31.0
    - alembic==1.18.4
    - pgvector==0.4.2
    # ── LLM 调用 ──────────────────────────────────────────────────────────────
    - openai==2.30.0
    - httpx==0.28.1
    # ── 本地 Embedding / Reranker ─────────────────────────────────────────────
    - sentence-transformers==5.5.1
    - transformers==5.9.0
    - torch==2.12.0       # CPU 版：安装后用 pip install torch --index-url https://download.pytorch.org/whl/cpu 替换
    - numpy==2.2.6
    - scikit-learn==1.7.2
    - huggingface-hub==1.16.0
    # ── 文档解析（知识库入库） ────────────────────────────────────────────────
    - pypdf==6.12.0
    - openpyxl==3.1.5
    # ── 工具库 ────────────────────────────────────────────────────────────────
    - python-dotenv==1.2.2
    - requests==2.33.1
    - pydantic==2.12.5
    - pyyaml==6.0.3
    - tqdm==4.67.3
